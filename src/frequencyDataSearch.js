var frequencyData = {};
function frequencyDataSearch(data) {
    (function frequency1() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("а", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["а"] = tmp.length / data.length;
    })();
    (function frequency2() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("б", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["б"] = tmp.length / data.length;
    })();
    (function frequency3() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("в", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["в"] = tmp.length / data.length;
    })();
    (function frequency4() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("г", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["г"] = tmp.length / data.length;
    })();
    (function frequency5() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("д", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["д"] = tmp.length / data.length;
    })();
    (function frequency6() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("е", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["е"] = tmp.length / data.length;
    })();
    (function frequency7() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("є", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["є"] = tmp.length / data.length;
    })();
    (function frequency8() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("ж", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["ж"] = tmp.length / data.length;
    })();
    (function frequency9() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("з", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["з"] = tmp.length / data.length;
    })();
    (function frequency10() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("и", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["и"] = tmp.length / data.length;
    })();
    (function frequency11() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("і", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["і"] = tmp.length / data.length;
    })();
    (function frequency12() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("ї", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["ї"] = tmp.length / data.length;
    })();
    (function frequency13() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("й", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["й"] = tmp.length / data.length;
    })();
    (function frequency14() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("к", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["к"] = tmp.length / data.length;
    })();
    (function frequency15() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("л", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["л"] = tmp.length / data.length;
    })();
    (function frequency16() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("м", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["м"] = tmp.length / data.length;
    })();
    (function frequency17() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("н", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["н"] = tmp.length / data.length;
    })();
    (function frequency18() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("о", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["о"] = tmp.length / data.length;
    })();
    (function frequency19() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("п", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["п"] = tmp.length / data.length;
    })();
    (function frequency20() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("р", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["р"] = tmp.length / data.length;
    })();
    (function frequency21() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("с", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["с"] = tmp.length / data.length;
    })();
    (function frequency22() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("т", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["т"] = tmp.length / data.length;
    })();
    (function frequency23() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("у", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["у"] = tmp.length / data.length;
    })();
    (function frequency24() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("ф", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["ф"] = tmp.length / data.length;
    })();
    (function frequency25() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("х", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["х"] = tmp.length / data.length;
    })();
    (function frequency26() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("ц", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["ц"] = tmp.length / data.length;
    })();
    (function frequency27() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("ч", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["ч"] = tmp.length / data.length;
    })();
    (function frequency28() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("ш", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["ш"] = tmp.length / data.length;
    })();
    (function frequency29() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("щ", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["щ"] = tmp.length / data.length;
    })();
    (function frequency30() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("ь", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["ь"] = tmp.length / data.length;
    })();
    (function frequency31() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("ю", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["ю"] = tmp.length / data.length;
    })();
    (function frequency32() {
        var pos = -1;
        tmp = [];
        while ((pos = data.indexOf("я", pos + 1)) != -1) {
            tmp.push(pos);
        }
        frequencyData["я"] = tmp.length / data.length;
    })();
}